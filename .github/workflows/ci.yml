format:
  name: Format Check
  runs-on: ubuntu-latest
  steps: 
    # Follow the pattern from the lint job above.
    # 1. Check out repository code
    # 2. Set up Node.js version 20
    # 3. Install dependencies with npm ci
    # 4. Run the format:check script

test:
  name: test
  runs-on: ubuntu-latest
  steps: 
    # Follow the pattern from the lint job above.
    # 1. Check out the reposirty code.
    # 2. Set up Node.js version 20
    # 3. Install dependencies with npm ci.
    # 4. Run the test script